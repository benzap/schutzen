(ns schutzen.core
  (:import (org.lwjgl Sys)
           (org.lwjgl.glfw GLFWErrorCallback
                           GLFWKeyCallback)
           (org.lwjgl.opengl)
  (:gen-class)))



(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
